<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://mwillisii.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mwillisii.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-08T17:07:07+00:00</updated><id>https://mwillisii.github.io/feed.xml</id><title type="html">Mark’s Notes</title><subtitle></subtitle><entry><title type="html">setting up nextcloud aio with nginx proxy manager and portainer</title><link href="https://mwillisii.github.io/blog/2025/nextcloud-npm/" rel="alternate" type="text/html" title="setting up nextcloud aio with nginx proxy manager and portainer"/><published>2025-11-20T12:00:00+00:00</published><updated>2025-11-20T12:00:00+00:00</updated><id>https://mwillisii.github.io/blog/2025/nextcloud-npm</id><content type="html" xml:base="https://mwillisii.github.io/blog/2025/nextcloud-npm/"><![CDATA[<p>It seems we are in a dark age of the Internet where enshitification is taking over everything. I’ve used Google for just about everything over the past decade or so - email, calendar, contacts, photos, etc. These services used to be solid, albeit not exactly privacy-focused. Not so much these days… AI is invading every service known to man, Google is snooping on everything you do to train their models, and who knows what else. So I’m finally taking the overdue plunge to self-host as much as possible from my own home.</p> <p>Both my wife and I were heavy Google Photos users - we have years and years of photos backed up. My first goal in this self-hosting adventure is to simply de-Google. Email is fairly easy - I’ve switched to mailbox.org, which allows using your own domain name for just a few bucks a month. Drive and Photos, on the other hand… not so simple. I decided on Nextcloud since it includes a replacement for all the Google services I used - photos, calendars, contacts, and notes. They also have both Android and iOS apps for auto-upload, which is important because my wife is not quite as tech-savvy, so I want the switch to be seamless for her.</p> <p>First things first, I needed hardware. I used to run a Raspberry Pi for Pi Hole and other small projects, but I knew I needed to upgrade. After a little research, I ended up going with <a href="https://www.gmktec.com/products/nucbox-g3-plus-enhanced-performance-mini-pc-with-intel-n150-processor?variant=22e83d5e-d20e-41a7-994f-e2aa05e5b6c8">this GMKtec mini-PC</a> with 16 GB of RAM and 1 TB NVME SSD. I figure 1 TB will be enough storage to get me started (even with all our photos, we only utilized ~200 GB of Photos/Drive storage). If I end up needing more storage in the future… well I don’t know, that’s a problem for future me. But this will hold us over for a while.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-11-20-nextcloud-npm(01)-480.webp 480w,/assets/img/2025-11-20-nextcloud-npm(01)-800.webp 800w,/assets/img/2025-11-20-nextcloud-npm(01)-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-11-20-nextcloud-npm(01).jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> My "home lab," complete with dog hair and a rat nest of cables! I really should tidy this up... </div> <p>This mini PC came with Windows, but I’m not a Microsoft fan, so I wiped it and installed Debian. Now that I’ve got all that out of the way, it’s time to install Nextcloud. I’m not reinventing the wheel here - there are lots of guides out there on how to install Nextcloud. The official method from Nextcloud themselves is to <a href="https://github.com/nextcloud/all-in-one">install Nextcloud AIO via Docker</a>. This is all well and good, and I even utilized this method for a few months while I got my feet wet. However, the problem I ran into is in regards to the rest of my hosted services on the same server: any service that uses the same port as Nextcloud is black-holed, since Nextcloud “hijacks” the port on the host to redirect it to the Docker container. And since Nextcloud uses both port 80 (http) and 443 (https), this essentially broke any other service using those ports. Considering over the past few months I’ve setup several self-hosted services on this box (Pi Hole, Plex, Sonarr, Radarr, Miniflux, etc.), this was problematic.</p> <p>This is where a reverse proxy comes in. Essentially, it takes traffic coming into the server and redirects it to the appropriate service. I went with Nginx Proxy Manager, since it has a nice little webpage GUI that makes configuration super easy.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/2025-11-20-nextcloud-npm(02)-480.webp 480w,/assets/img/2025-11-20-nextcloud-npm(02)-800.webp 800w,/assets/img/2025-11-20-nextcloud-npm(02)-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/2025-11-20-nextcloud-npm(02).png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> My Nginx Proxy Manager instance </div> <p>There may be better options here, but I’m a sucker for ease-of-use, and NPM fits the bill. So off I go searching for guides on installing Nextcloud behind NPM, and I came across <a href="https://github.com/nextcloud/all-in-one/discussions/4240">this amazing guide from the Nextcloud AIO GitHub wiki</a> (thanks <a href="https://github.com/4lexRed">4lexRed</a>!) In this post, 4lexRed includes a Docker compose file, which is used by Docker to provision both Nextcloud and NPM, as well as Portainer (a web-hosted GUI to manage Docker containers, because I loathe managing Docker from the CLI), all from one Docker command. There are a few changes that need to be made to the compose file for my environment, all of which were mentioned in their instructions.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># sample compose for a nextcloud all in one container, running behind a nginx proxy manager</span>
<span class="c1"># (optionally) uses portainer to give a better overview of the docker environment and the running containers</span>
<span class="c1">#</span>
<span class="c1"># the network here is using fixed ip addesses for some hosts, as the nextcloud setting</span>
<span class="c1"># NC_TRUSTED_PROXIES needs an ip address to relate to.</span>
<span class="c1">#</span>
<span class="c1"># Note: the data of npm and portainer container are stored in local folder instead of volumes.</span>
<span class="c1">#       you need to create these folders or comment these lines</span>
<span class="c1">#</span>
<span class="c1"># questions? post to https://github.com/nextcloud/all-in-one/discussions/4240</span>
<span class="c1">#</span>
<span class="c1"># 4lexRed</span>

<span class="na">services</span><span class="pi">:</span>
  <span class="na">npm</span><span class="pi">:</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">npm</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s1">'</span><span class="s">jc21/nginx-proxy-manager:latest'</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s1">'</span><span class="s">always'</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="c1"># These ports are in format &lt;host-port&gt;:&lt;container-port&gt; - both MUST be open</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">80:80'</span> <span class="c1"># Public HTTP Port  - [I myself disable port 80 after the setting up the ssl certificates - as most traffic is https nowadays]</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">443:443'</span> <span class="c1"># Public HTTPS Port</span>

      <span class="c1"># Admin Web Port - disable as soon as the NPM is running and you set up a route on localhost and port 81</span>
      <span class="c1"># see here https://github.com/NginxProxyManager/nginx-proxy-manager/issues/1658</span>
      <span class="c1">#</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">81:81'</span>   <span class="c1"># enable if you want to access the npm service via [your.server.ip.address:81]</span>

      <span class="c1"># Add any other Stream port you want to expose</span>
      <span class="c1"># - '21:21' # FTP</span>

    <span class="na">volumes</span><span class="pi">:</span>
      <span class="c1"># These volumes are in format &lt;local-dir&gt;:&lt;container-dir&gt;</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">/home/mark/nginx-proxy-manager/data:/data'</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">/home/mark/nginx-proxy-manager/letsencrypt:/etc/letsencrypt'</span>
      <span class="c1"># - '/opt/npm_logs/etc:/etc/nginx/conf.d:ro'  # for ip ban</span>
      <span class="c1"># - '/opt/npm_logs/logs:/var/log/nginx:rw'  # for ip-ban</span>

    <span class="na">environment</span><span class="pi">:</span>
      <span class="c1"># Uncomment this if you want to change the location of the SQLite DB file within the container</span>
      <span class="c1"># DB_SQLITE_FILE: "/data/database.sqlite</span>

      <span class="c1"># changing the user id may lead to PORT 80 not being reachable</span>
      <span class="c1"># PUID: 1000</span>
      <span class="c1"># PGID: 1000</span>

      <span class="c1"># DISABLE_IPV6: 'true'  # Uncomment this if IPv6 is not enabled on your host (WAN interface)</span>
      <span class="na">TZ</span><span class="pi">:</span> <span class="s2">"</span><span class="s">America/New_York"</span>   <span class="c1"># the timezone of the server</span>

    <span class="na">healthcheck</span><span class="pi">:</span>
      <span class="na">test</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">CMD"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">/usr/bin/check-health"</span><span class="pi">]</span>
      <span class="na">interval</span><span class="pi">:</span> <span class="s">10s</span>
      <span class="na">timeout</span><span class="pi">:</span> <span class="s">3s</span>

    <span class="c1"># Either use netmorke mode or networks.</span>
    <span class="c1"># Both at the same time is not possible.</span>
    <span class="c1"># Nextcloud AIO recommends in their guide to set it to network_mode: host.</span>
    <span class="c1"># https://github.com/nextcloud/all-in-one/blob/main/reverse-proxy.md#adapting-the-sample-web-server-configurations-below</span>

    <span class="c1"># But I dislike to do this and instead have all traffic going through the npm using the networks approach.</span>

    <span class="c1"># network_mode: host  # for details in the docs see https://docs.docker.com/compose/compose-file/05-services/#network_mode</span>

    <span class="na">networks</span><span class="pi">:</span> <span class="c1"># Is needed when you want to create the nextcloud-aio network with ipv6-support using this file, see the network config at the bottom of the file</span>
      <span class="na">nextcloud-aio</span><span class="pi">:</span>
        <span class="na">aliases</span><span class="pi">:</span>
         <span class="pi">-</span> <span class="s">npm</span>
        <span class="na">ipv4_address</span><span class="pi">:</span> <span class="s1">'</span><span class="s">172.18.0.2'</span>

<span class="c1"># # # # # # # # # # # #</span>
<span class="c1"># portainer usage is optional. you can delete these lines if you are fine with monitoring the state via the cli.</span>

  <span class="na">portainer</span><span class="pi">:</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">portainer</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">portainer/portainer-ce:2.20.2</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">security_opt</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">no-new-privileges:true</span>

    <span class="c1"># ports:</span>
    <span class="c1"># when uncommented, these ports are publicly accessable [bypassing the npm] by calling [your.server.ip.address:9000]</span>
    <span class="c1"># so disable them when the npm is set up to forward the port 9443 when calling docker.myhostname.com (or whichever your subdomain for the portainer is)</span>
      <span class="c1"># - 9000:9000</span>
      <span class="c1"># - 9443:9443</span>

    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">/etc/localtime:/etc/localtime:ro</span>
      <span class="pi">-</span> <span class="s">/var/run/docker.sock:/var/run/docker.sock:ro</span>
      <span class="pi">-</span> <span class="s1">'</span><span class="s">/docker_data/portainer_data:/data'</span>  <span class="c1"># the data folder of the portainer</span>

    <span class="na">networks</span><span class="pi">:</span>
      <span class="na">nextcloud-aio</span><span class="pi">:</span>
        <span class="na">aliases</span><span class="pi">:</span>
         <span class="pi">-</span> <span class="s">portainer</span>
        <span class="na">ipv4_address</span><span class="pi">:</span> <span class="s1">'</span><span class="s">172.18.0.3'</span>


<span class="c1"># # # # # # # # # # # #</span>
<span class="c1"># The nextcloud All in one container</span>
<span class="c1"># see https://github.com/nextcloud/all-in-one#faq</span>

  <span class="na">nextcloud</span><span class="pi">:</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">nextcloud/all-in-one:latest</span>
    <span class="na">init</span><span class="pi">:</span> <span class="kc">true</span>
    <span class="na">restart</span><span class="pi">:</span> <span class="s">always</span>
    <span class="na">container_name</span><span class="pi">:</span> <span class="s">nextcloud-aio-mastercontainer</span>  <span class="c1"># This line is not allowed to be changed as otherwise AIO will not work correctly</span>

    <span class="na">volumes</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">nextcloud_aio_mastercontainer:/mnt/docker-aio-config</span> <span class="c1"># This line is not allowed to be changed as otherwise the built-in backup solution will not work</span>
      <span class="pi">-</span> <span class="s">/var/run/docker.sock:/var/run/docker.sock:ro</span> <span class="c1"># May be changed on macOS, Windows or docker rootless. See the applicable documentation. If adjusting, don&gt;</span>

    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="s">8080:8080</span>  <span class="c1"># this is the port of the admin interface used for the setup of NC-AIO</span>
      <span class="c1"># - 5080:80 # Can be removed when running behind a web server or reverse proxy (like Apache, Nginx, Cloudflare Tunnel and else). See https://github.com/n&gt;</span>
      <span class="c1"># - 8443:8443 # Can be removed when running behind a web server or reverse proxy (like Apache, Nginx, Cloudflare Tunnel and else). See https://github.com&gt;</span>

    <span class="na">environment</span><span class="pi">:</span>
      <span class="c1"># - NEXTCLOUD_TRUSTED_DOMAINS = nextcloud.myhostname.com   # the dns name under which your nextcloud should be accessable</span>

      <span class="pi">-</span> <span class="s">APACHE_PORT=11000</span> <span class="c1"># Is needed when running behind a web server or reverse proxy (like Apache, Nginx, Cloudflare Tunnel and else).</span>
                          <span class="c1"># See https://github.com/nextcloud/all-in-one/blob/main/reverse-proxy.md</span>

      <span class="pi">-</span> <span class="s">APACHE_IP_BINDING=0.0.0.0</span> <span class="c1"># Should be set to '0.0.0.0' when running behind a web server or reverse proxy (like Apache, Nginx, Cloudflare Tunnel and els&gt;</span>
                                  <span class="c1"># See https://github.com/nextcloud/all-in-one/blob/main/reverse-proxy.md</span>
                                  <span class="c1"># If set to 0.0.0.0 the apache will listen to all http requests, no matter what their target ip address is</span>

      <span class="pi">-</span> <span class="s">NEXTCLOUD_UPLOAD_LIMIT=10G</span> <span class="c1"># Can be adjusted if you need more. See https://github.com/nextcloud/all-in-one#how-to-adjust-the-upload-limit-for-nextcloud</span>
      <span class="c1"># - NEXTCLOUD_DATADIR=/mnt/nfs-storage/my_folder/nextcloud_data   # Allows to set the host directory for Nextcloud's datadir. ⚠⚠⚠ Warning: do not set or &gt;   # - NEXTCLOUD_MOUNT=/mnt/nfs-storage/my_folder/nextcloud_data2  # Allows the Nextcloud container to access the chosen directory on the host. See https://&gt;</span>

      <span class="c1"># settings overwriting the config.php parameters</span>
      <span class="pi">-</span> <span class="s">NC_TRUSTED_PROXIES="172.18.0.2"</span>  <span class="c1"># this is the NPM proxy ip address in the docker network !</span>

      <span class="c1"># - AIO_DISABLE_BACKUP_SECTION=false # Setting this to true allows to hide the backup section in the AIO interface. See https://github.com/nextcloud/all-&gt;</span>
      <span class="c1"># - BORG_RETENTION_POLICY=--keep-within=7d --keep-weekly=4 --keep-monthly=6 # Allows to adjust borgs retention policy. See https://github.com/nextcloud/a&gt;</span>
      <span class="c1"># - COLLABORA_SECCOMP_DISABLED=false # Setting this to true allows to disable Collabora's Seccomp feature. See https://github.com/nextcloud/all-in-one#ho&gt;</span>
      <span class="c1"># - NEXTCLOUD_MAX_TIME=3600 # Can be adjusted if you need more. See https://github.com/nextcloud/all-in-one#how-to-adjust-the-max-execution-time-for-next&gt;</span>
      <span class="c1"># - NEXTCLOUD_MEMORY_LIMIT=512M # Can be adjusted if you need more. See https://github.com/nextcloud/all-in-one#how-to-adjust-the-php-memory-limit-for-ne&gt;</span>
      <span class="c1"># - NEXTCLOUD_TRUSTED_CACERTS_DIR=/path/to/my/cacerts # CA certificates in this directory will be trusted by the OS of the nexcloud container (Useful e.g&gt;</span>
      <span class="c1"># - NEXTCLOUD_STARTUP_APPS=deck twofactor_totp tasks calendar contacts notes # Allows to modify the Nextcloud apps that are installed on starting AIO the&gt;</span>
      <span class="c1"># - NEXTCLOUD_ADDITIONAL_APKS=imagemagick # This allows to add additional packages to the Nextcloud container permanently. Default is imagemagick but can&gt;</span>
      <span class="c1"># - NEXTCLOUD_ADDITIONAL_PHP_EXTENSIONS=imagick # This allows to add additional php extensions to the Nextcloud container permanently. Default is imagick&gt;</span>
      <span class="c1"># - NEXTCLOUD_ENABLE_DRI_DEVICE=true # This allows to enable the /dev/dri device in the Nextcloud container. ⚠⚠⚠ Warning: this only works if the '/dev/dr&gt;   # - NEXTCLOUD_KEEP_DISABLED_APPS=false # Setting this to true will keep Nextcloud apps that are disabled in the AIO interface and not uninstall them if t&gt;</span>
      <span class="c1"># - TALK_PORT=3478 # This allows to adjust the port that the talk container is using. See https://github.com/nextcloud/all-in-one#how-to-adjust-the-talk-&gt;</span>
      <span class="c1"># - WATCHTOWER_DOCKER_SOCKET_PATH=/var/run/docker.sock # Needs to be specified if the docker socket on the host is not located in the default '/var/run/d&gt;</span>

    <span class="na">networks</span><span class="pi">:</span> <span class="c1"># Is needed when you want to create the nextcloud-aio network with ipv6-support using this file, see the network config at the bottom of the file</span>
      <span class="na">nextcloud-aio</span><span class="pi">:</span>
        <span class="na">aliases</span><span class="pi">:</span>
         <span class="pi">-</span> <span class="s">nextcloud</span>
        <span class="na">ipv4_address</span><span class="pi">:</span> <span class="s1">'</span><span class="s">172.18.0.4'</span>

<span class="na">volumes</span><span class="pi">:</span> <span class="c1"># If you want to store the data on a different drive, see https://github.com/nextcloud/all-in-one#how-to-store-the-filesinstallation-on-a-separate-dri&gt;</span>
  <span class="na">nextcloud_aio_mastercontainer</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">nextcloud_aio_mastercontainer</span> <span class="c1"># This line is not allowed to be changed as otherwise the built-in backup solution will not work</span>

<span class="na">networks</span><span class="pi">:</span>
  <span class="na">nextcloud-aio</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">nextcloud-aio</span>
    <span class="na">ipam</span><span class="pi">:</span>
      <span class="na">driver</span><span class="pi">:</span> <span class="s">default</span>
      <span class="na">config</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">subnet</span><span class="pi">:</span> <span class="s">172.18.0.0/24</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="docker"/><category term="nextcloud"/><category term="nginx"/><category term="proxy"/><category term="npm"/><category term="portainer"/><summary type="html"><![CDATA[a guide for configuring nextcloud aio to function behind nginx proxy manager, including a docker compose file for setting up nextcloud, npm, and portainer]]></summary></entry></feed>